# Procfile tells deployment platforms (like Heroku) how to run your app
# web: defines a web process type that can receive HTTP traffic
# gunicorn: WSGI server for production (replaces Flask's development server)
# main:app: tells Gunicorn to look for 'app' variable in 'main.py' file
web: gunicorn main:app
